const initialState = {
  referenceFPS: 60
}

function circlet(state = initialState, action) {
  const { type, payload } = action;

  switch(type) {
    default:
      return state;
  }
}

export default circlet;
